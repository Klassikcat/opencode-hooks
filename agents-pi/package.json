{
  "name": "agents-pi",
  "version": "0.1.0",
  "description": "Oh My OpenAgent-inspired workflow skills and provider offload adapters for pi coding agent.",
  "private": true,
  "type": "module",
  "bin": {
    "omo-offload": "./src/cli.js"
  },
  "scripts": {
    "check": "node --check src/cli.js && node --test",
    "test": "node --test"
  },
  "keywords": ["pi-package", "pi", "skills", "coding-agent"],
  "pi": {
    "skills": ["./skills"]
  }
}
