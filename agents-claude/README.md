# agents-claude

Claude Code subagent definitions. Install by copying `agents/*.md` into `~/.claude/agents/` or a project `.claude/agents/` directory. These files are generated from `agents-tester/roles/`; edit the canonical role files there, then run `node agents-tester/bin/generate-agents.mjs` instead of hand-editing generated agents.
